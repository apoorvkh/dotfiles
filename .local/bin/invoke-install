#!/bin/env zsh
read "yn?Are you sure that you want to install your dotfiles to $@? (y/n) "

if [ "$yn" = "y" ]; then
    ssh $@ 'wget https://raw.githubusercontent.com/apoorvkh/dotfiles/main/.local/bootstrap.sh -O - | sh';
fi
